import 'package:flutter/cupertino.dart';

const EdgeInsets appHorizontalPadding = EdgeInsets.symmetric(horizontal: 15);
const EdgeInsets appVerticalPadding = EdgeInsets.symmetric(vertical: 10);
const BorderRadius appSmallBorderRaduis = BorderRadius.all(
  Radius.circular(7),
);
const BorderRadius appNormalBorderRaduis = BorderRadius.all(
  Radius.circular(10),
);
const BorderRadius appLargeBorderRaduis = BorderRadius.all(
  Radius.circular(13),
);
